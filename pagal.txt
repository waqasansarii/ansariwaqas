learning git
1) install git using git-scm.com
2) github.com(making profile)
3) making repo githu.com
4) git init (for making local folderinto repo)
5) git status (for checking status)
6) git add -A (for making filestrackable)
7) git config --global user.name"apna naam"
  git config --global user.email"apni email"
8) git commit -m "jo kaam apnay kya hay"